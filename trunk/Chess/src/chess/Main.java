package chess;

public class Main {

        public static void main(String[] args) {
        	int turno = 0;
            int origenX;
            int origenY;
            int destinoX;
            int destinoY;
            boolean respuesta;

            /*try {
                while(true) {
                    ajedrez.mostrarTablero();
                    System.out.println("Juega "+jugadores[turno%2]);

                    do{
                        System.out.print("Elija Pieza\nPosX: ");
                        origenX = Integer.parseInt(inputStream.readLine());
                        System.out.print("PosY: ");
                        origenY = Integer.parseInt(inputStream.readLine());
                        System.out.print("Elija destino\nPosX: ");
                        destinoX = Integer.parseInt(inputStream.readLine());
                        System.out.print("PosY: ");
                        destinoY = Integer.parseInt(inputStream.readLine());
                        respuesta = ajedrez.realizarJugada(jugadores[turno % 2], origenX, origenY, destinoX, destinoY);
                        if(respuesta) {

                        } else {
                            System.out.println("Movimiento Invalido");
                        }
                    } while (!respuesta);


                    turno++;
                }
            } catch(Exception e) {
            }*/
        }
        	
    }

}
